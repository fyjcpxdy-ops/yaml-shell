先创建secrets
kubectl create secret docker-registry aliyun \
    --docker-server=crpi-9zzmolgnmooojeev.cn-beijing.personal.cr.aliyuncs.com \
    --docker-username=漠然回首时 \
    --docker-password=Xu200287.com \
    --docker-email=2041828712@qq.com

apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "3"
  generation: 3
  labels:
    app: test
  name: test
  namespace: default
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: test
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: test
    spec:
      containers:
      - image: crpi-9zzmolgnmooojeev.cn-beijing.personal.cr.aliyuncs.com/container_app/nginx:1.21
        imagePullPolicy: IfNotPresent   #
        name: ingress-nginx-kube-webhook-certgen
        resources: {}
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      imagePullSecrets:   # 引用 Secret认证
      - name: aliyun  # Secret 的名称
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
